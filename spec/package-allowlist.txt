# spec/package-allowlist.txt
#
# Machine-readable list of paths allowed in a published crates.io tarball of
# zai-rs (plan P00.12 / P14.7). xtask `package check` runs
# `cargo package --list --allow-dirty`, sorts the output, and compares it
# against this file exactly — any extra path fails the gate.
#
# Only what a downstream consumer needs to use the crate is shipped: source,
# the two user-facing docs, license and changelog, and the cargo metadata
# files. spec/, tests/, fuzz/, xtask/, examples/, release/ and target/ are
# excluded.
.gitignore
CHANGELOG.md
Cargo.lock
Cargo.toml
Cargo.toml.orig
.cargo_vcs_info.json
LICENSE
README.md
docs/GETTING_STARTED.md
docs/MIGRATING-0.5.md
src/**/*.rs
