# Pin local development to the crate's minimum supported Rust version. CI also
# tests stable and nightly independently; llvm-tools supports `cargo llvm-cov`.

[toolchain]
channel = "1.88.0"
components = ["rustfmt", "clippy", "llvm-tools-preview"]
profile = "minimal"
