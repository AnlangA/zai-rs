# Pin local development to the crate's minimum supported Rust version. CI tests
# stable and nightly independently and installs llvm-tools in its coverage job.
# Local coverage runs can add `llvm-tools-preview` to this toolchain explicitly.

[toolchain]
channel = "1.88.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
