# Match `target/` at any depth — root build dir plus nested ones such as
# `examples/web_chat/target/`.
target/
.idea/
/out
/.claude
/.zcode
/realtime_out.bin

# Generated artifacts — never commit
*.cdx.json
target/sbom/
target/coverage*.json
target/release-evidence/
