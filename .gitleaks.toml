# Inherit the default rules and exclude generated dependency/build trees.

title = "zai-rs gitleaks config"

[allowlist]
description = "Global allowlist — exclude build output and caches."
paths = [
    '''(?:^|/)(?:target|node_modules)(?:/|$)''',
]
